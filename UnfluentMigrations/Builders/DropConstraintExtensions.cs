using UnfluentMigrations.Operations;
using UnfluentMigrations.Operations.Model;

namespace UnfluentMigrations.Builders
{
    public static class DropConstraintExtensions
    {
        //public static OperationBuilderSurface<DropConstraintOperation> DropConstraint(this IMigrationBuilder builder, string name, string table, string schema = null, string catalog = null)
        //{
        //    var op = new DropConstraintOperation
        //    {
        //        Name = new SubObjectName(catalog, schema, table, name)
        //    };
        //    builder.AddOperation(op);
        //    return new OperationBuilderSurface<DropConstraintOperation>(op);
        //}
    }
}